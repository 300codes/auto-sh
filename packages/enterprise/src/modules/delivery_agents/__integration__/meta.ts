export const integrationMeta = {
  description: 'Delivery agents enterprise integration coverage — execution bridge, workers, result acceptance',
  dependsOnModules: ['delivery_agents', 'delivery_os'],
  requiredEnvVars: ['OM_ENABLE_ENTERPRISE_MODULES'],
}
