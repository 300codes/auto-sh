# Research — T046 FLOW-F1 L13c gate call sites

- Gate rule exists and is pure: `lib/flowRules.ts#checkFlowGate(states, requiredStages, { v1Compatible })` → `{ ok:false, status 422, body { code: baseline_not_approved, details[{ path: stages.<id>, code ∈ FLOW_GATE_DETAIL_CODES, message: 'Stage <id> is <currency>' }] } }`; `computeStageCurrency(template, artifactRecords, decisionRecords)` derives currency for the four approval stages.
- Row loaders already live privately in `commands/stages.ts` (`loadStageArtifacts`, `loadStageDecisions`, `toArtifactRecord`, `toStoredDecision` → `clientApproved` = approved + approver name present). They move to `commands/flowGate.ts` and stages.ts imports them.
- Pinned marker: `DeliveryProject.flowTemplateId/flowTemplateVersion/flowTemplateHash/flowTemplateSnapshot/flowPinnedAt` written together by `delivery_os.flow.pin`. The gate keys on `flowTemplateId !== null` only; a non-null id with an unreadable snapshot fails closed (every stage reported `stage_not_approved`).
- Call sites: `tasks.ts#checkReadyGate` (returns `DeliveryCheckResult`, v1 reasons first), `attempts.ts` reserve (one path serves `manual_handoff` and trusted `automatic`; replay of an existing key returns before the lock and must stay untouched), `decisions.ts#recordDeployDecision` (after `report_not_green`, before `tx.persist`).
- Type bridge: with `v1Compatible` the body code is the v1 `baseline_not_approved`, so the result can be re-wrapped with `buildDeliveryError` into a `DeliveryCheckResult` and thrown by the existing `assertDeliveryCheck`.
- Test kits: `commands/__tests__/baselineTestKit.ts` (store, harness, `expectFrozenBody`, `detailCodes`), stage-row store extension pattern in `stages.test.ts`, green-report evidence rows in `decisions.test.ts`, trusted `automatic` reserve in `attempts.test.ts` (`issueTrustedExecution`, `inProcess`).
- Guards to keep green: `appendOnly.test.ts` (no property assignment on `*decision*`/`*baseline*`/`*evidence*` variables in command sources), `scopeChange.test.ts` (pins command ids; no new command here).
