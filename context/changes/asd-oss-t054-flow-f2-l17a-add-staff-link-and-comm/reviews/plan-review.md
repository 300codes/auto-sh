# Plan review — FLOW-F2 L17a (staff.link, comments.triage, stage thread seams)

Paths relative to `packages/core/src/modules/`. Spec = `.ai/specs/2026-09-18-delivery-os-hackathon.md`.

| # | Sev | Finding | Evidence | Suggested fix |
|---|---|---|---|---|
| 1 | HIGH | `canManageAll` path accepts ANY uuid: the resolver returns `projectIds: []` for managers, so a non-existent, soft-deleted or other-tenant staff project id is stored as a valid link (spec demands `404` for "foreign"). D3's "no oracle" holds, but existence/tenancy is never proven; L17b would then write cards against a dangling/foreign id. | `staff/lib/time-tracking/access.ts:124`; `staff/AGENTS.md:26`; spec:500 (F10); plan D1/D3 | Add a tenant+org scoped existence probe without a staff import (DI `queryEngine` on entity id `staff:staff_time_project`, `deletedAt: null`); absent or probe unavailable → same `404 not_found` body. Add a test: manager + unknown uuid → 404. |
| 2 | HIGH | D8 omits the `linkedDeliveryTaskId` check. Spec F13 lists `422 foreign_reference` for `deferral.artifactId` **and** `linkedDeliveryTaskId` of another project; as planned a task id of another project/tenant is persisted unverified. | spec:503 (F13); `delivery_os/lib/contracts.ts:1737`; plan D8 | Load the task with `{ id, projectId, tenantId, organizationId }` (`requireScopedTask`-style, `commands/shared.ts:208`); miss → `foreign_reference` (identical for foreign/missing). Define `undefined` = keep, `null` = clear. Test both. |
| 3 | HIGH | No row-lock story between F13 and F8. Triage locks only the thread version, F8 only the project; F8 reads threads un-locked (`planDecisionFor`) and `recordThreadDeferrals` overwrites rows blind. A triage `deferred→new` (or a re-triage) landing between F8's plan and commit yields an approval over an open thread, or a lost triage write. | `delivery_os/commands/stages.ts:419,519-549`; plan D7/D11 | In the F8 tx load stage threads `PESSIMISTIC_WRITE` after `lockScopedProject` and plan from those rows; triage loads its thread `PESSIMISTIC_WRITE` inside `em.transactional` before `enforceCommandOptimisticLockWithGuards`. Lock order project → thread (triage takes thread only), so no deadlock. |
| 4 | MEDIUM | D11 "update all stage threads with a deferred key" over-writes: `threadKey` is unique per `(project, source, fileKey)`, so a same-key thread in another file that is resolved, deleted or bound to another artifact gets `triageStatus: deferred` + a deferral it never earned. `ThreadDeferral`/`CommentThreadRecord` carry no row identity. | `delivery_os/data/entities.ts:612-616`; `delivery_os/lib/stageDecisions.ts:24-31,48,271-273` | Add `threadId` to `CommentThreadRecord` and `ThreadDeferral` (internal types, additive) and write by id; or re-apply `blockingThreadsFor` in the writer. Test: same key in two files, only the blocking one changes. |
| 5 | MEDIUM | Triage thread load scoping is not stated. It must be `{ id: threadId, projectId, tenantId, organizationId }` after `requireScopedProject`; a thread of another project/tenant → the same `404 not_found`. `threadId` alone would let a caller triage across projects. | plan §Phase 1 item 2; spec:492-494 ("foreign or missing record → 404") | State the filter and the 404 in the plan; add tests for foreign-project and foreign-tenant thread ids (identical body). |
| 6 | MEDIUM | Project lock for `staff.link` is only meaningful if the write bumps `project.updatedAt` (as pin does). Plan is silent; without the bump two serialized re-links both pass with the same stale header. | `delivery_os/commands/flow.ts:144-153`; spec:483 ("optimistic lock (project)") | Set `project.updatedAt = now` on create/re-link (not on `unchanged`); return/test the new project version for the L18 route. |
| 7 | MEDIUM | D2 stretches `staff_link_required`: spec lists it only for F11 ("no link yet"), not F10. Reusing it for "staff module absent" makes one code mean two things for L18/UI clients. | spec:500-501; `delivery_os/lib/contracts.ts:941` | Keep the code (additive set is frozen) but fix a distinct `details[].code` (e.g. `staff_module_unavailable`) in the plan and record the F10 addition in the spec row/changelog. |
| 8 | MEDIUM | Check order is unspecified, and it decides the oracle question: D6 replay and D5 `staff_project_already_linked` must both run AFTER the staff access check, otherwise `200 unchanged`/`409` vs `404` confirms a guessed staff project id to a caller without staff access. | plan D5/D6; spec:500 | Fix the order in the plan: scope project → staff access (404) → replay (200) → lock header → tx (relink rules, unique pre-check, unique-violation recovery). Test: inaccessible id that is already linked elsewhere → 404, not 409. |
| 9 | LOW | D8 leaves an unresolved question in the plan; the code exists: `hash_mismatch: 422` in the v1 set. | `delivery_os/lib/contracts.ts:64`; plan:34,40 | Replace the hedge with `buildDeliveryError('hash_mismatch', …)`; delete the `subject_hash_mismatch` aside. |
| 10 | LOW | D1 hard-codes staff's `staff.timesheets.projects.manage` inside delivery_os and re-derives via `getGrantedFeatures`; staff's guidance names `userHasAllFeatures` as the authority. Works (superadmin → `['*']`, `auth/services/rbacService.ts:528`), RBAC failure degrades to memberships (closed). | `staff/AGENTS.md:58-62`; `staff/lib/time-tracking/access.ts:103-108` | Keep, but name the constant, note the coupling in the hand-over, and test: RBAC throws → member still links own project, manager-only caller gets 404. |
| 11 | LOW | End-state wording "un-triaged" is looser than the rule: `triaged` threads still block (only `resolved` / validly `deferred` are exempt). Item 3 of Phase 1 also keeps a self-contradicting note about where loaders live. | `delivery_os/lib/stageDecisions.ts:101-108`; spec:545-550; plan:52 | Add a `stages.test.ts` case for a `triaged` open thread blocking; state once: loaders stay in `stages.ts`. |
| 12 | LOW | Thread mutation in `stages.ts`/`comments.ts` sits next to decision variables; the static scan flags `*decision*.prop =` and any `assign(`. | `delivery_os/commands/__tests__/appendOnly.test.ts:46-52` | Name rows `thread`/`threadRow`, set fields one by one, never `em.assign`; keep `buildLog` free of `author`/`body` (PII). |

**Verdict:** REVISE — feasible and contract-aligned, but fix #1-#3 (unverified staff id under `canManageAll`, missing `linkedDeliveryTaskId` scope check, F8/F13 thread race) and pin down #4-#8 before implementing; note `commands/staffLink.ts` already exists untracked and currently reproduces #1 (it already handles #6 and the #8 order, so only the plan text lags there).
