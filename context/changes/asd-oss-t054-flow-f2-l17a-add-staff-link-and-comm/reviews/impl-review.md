# Impl review — FLOW-F2 L17a (staff.link, comments.triage, F8 thread seams)

Paths relative to `packages/core/src/modules/delivery_os/`. Read-only review of the uncommitted diff; nothing was executed.
Checked OK: every query carries `tenantId`+`organizationId` (+`projectId`); check order project → staff access (404) → replay → lock header → tx (plan-review #8); manager probe via `queryEngine` fails closed (#1); RBAC/resolver failure → memberships only / 404; lock order project → thread in both F8 and F13 (#3); per-row `blockingThreadsFor` re-check (#4); `triaged` blocks (#11); each `resolveDeliveryEm` is a fresh fork, so the locked reads are not served stale from the probe's identity map; mutate-after-decrypt is the same pattern as `commands/intake.ts:151-159` (subscriber re-baselines after decrypt and re-encrypts `author`/`body` in `beforeUpdate`); no staff import, no `any`, no `em.assign`, audit snapshots carry no PII.

| # | Sev | Finding | File:line | Fix |
|---|---|---|---|---|
| 1 | MEDIUM | No test proves the locking that plan-review #3 demanded: nothing asserts `PESSIMISTIC_WRITE` on the F8 thread load / triage thread load, nor the project-before-thread order. The in-memory kit ignores `lockMode`, so removing `lockThreads=true` or `lockScopedProject` in triage keeps every suite green. | `commands/stages.ts:571`, `commands/comments.ts:116-117`, `__tests__/comments.test.ts`, `__tests__/stages.test.ts:676` | Spy on the mocked `findWithDecryption`/`findOneWithDecryption`: assert options `lockMode` for `DeliveryCommentThread` inside the tx and that the `DeliveryProject` locked call precedes it (both commands). |
| 2 | MEDIUM | F8 loads and decrypts all stage threads twice per approval with deferrals (`planDecisionFor` locked load, then `recordThreadDeferrals` loads again). The second `find` returns the already-managed rows (no refresh), so it buys nothing, and it decrypts `author`/`body` a second time although the writer needs no PII. | `commands/stages.ts:193`, `:468` | Return the locked rows from planning (e.g. `DecisionPlanning.threadRows`) and pass them to `recordThreadDeferrals`; drop the second query. |
| 3 | LOW | Member path skips the existence probe: `projectIds` comes from membership rows (`deletedAt: null` on the membership only), so a soft-deleted staff project with a live membership is linkable; only the `canManageAll` path is probed. | `commands/staffLink.ts:126-127` | Run `staffProjectExists` for both paths (`(inList \|\| canManageAll) && exists`); add a test. |
| 4 | LOW | Triage reuses `requireLockHeader`, whose body says "The project version header is required" while F13 needs the thread version — misleading for L18/UI clients. | `commands/comments.ts:112`, `commands/shared.ts:88` | Add an optional message/subject argument (default unchanged) and pass "thread version". |
| 5 | LOW | `assertDeferralBinding` accepts any artifact of the stage: an older version, or one different from the thread's confirmed `artifactId`. Harmless for F8 (such a deferral never unblocks the current hash) but stores a `deferred` status that still blocks — confusing in the L18 UI. | `commands/comments.ts:57-70` | When `thread.artifactId` is set require `deferral.artifactId === thread.artifactId` (`foreign_reference`), or record the accepted behaviour in the spec row. |
| 6 | LOW | `assertRelinkAllowed` reads "any carded thread" without a lock; it is only safe if the L17b import takes the project row lock before creating cards. Not stated anywhere. | `commands/staffLink.ts:174-183` | Note the invariant in the L17b hand-over (import must `lockScopedProject` first); add the race test there. |
| 7 | LOW | Same `threadKey` blocking in two files: one `deferredThreadKeys` entry defers both rows, the stored decision lists the key twice-or-once depending on `planStageDecision`. Untested. | `commands/stages.ts:194-205`, `lib/stageDecisions.ts:271-284` | Add a test with two blocking same-key rows; dedupe the stored keys if needed. |
| 8 | LOW | Test gaps: triage foreign-tenant thread (only foreign org is covered), `linkedDeliveryTaskId: null` clearing, soft-deleted linked task, F8 duplicate replay not re-writing deferrals, `unchanged` result producing no audit log. | `__tests__/comments.test.ts:135,178,184`, `__tests__/staffLink.test.ts:190` | Add the five cases. |
| 9 | LOW | The kit's `$ne` matcher treats a row without the field (`undefined`) as "not null", unlike SQL `IS NOT NULL`; a fixture missing `staffTaskId` would read as carded. | `__tests__/baselineTestKit.ts:73-75` | Compare with `(actual ?? null) !== (expected.$ne ?? null)`. |
| 10 | LOW | `plan.md` Progress boxes are all unchecked and the plan text still carries the D8 hedge and the self-contradicting Phase 1 item 3 (plan-review #9, #11). | `plan.md:34,40,52,80-88` | Tick progress with the commit sha; clean the two notes. |

**Verdict:** APPROVE WITH FOLLOW-UPS — no scoping, oracle or fail-open defect found and plan-review #1, #3, #4, #8, #11 are implemented; add the lock assertions (#1) and drop the double thread load (#2) before L17b builds on these seams.

**Triage (T054):** fixed #1 (lock-order/lock-mode assertions in `comments.test.ts` and `stages.test.ts`), #2 (locked rows passed from planning to `recordThreadDeferrals`, second query removed), #3 (existence probe on both paths + test), #7 (stored keys deduped + two-file test), #10 (plan cleaned, progress ticked; rows stay SHA-less — orchestrator commits). #9 dismissed: `matches` already normalises `actual` with `?? null`. Deferred to L17b/L18 with a hand-over note: #4 (lock-header wording), #5 (older-artifact deferral is accepted and harmless), #6 (import must take the project lock first), #8 (remaining test cases ride with the route/integration specs).
